#include <iostream>
#include <vector>
using namespace std;

int main()
{
    int T;
    cin >> T;
    while (T--)
    {
        vector<int> h;
        int S, buff;
        cin >> S;
        for (int i = 0; i < S; i++)
        {
            cin >> buff;
            h.push_back(buff);
        }
    }
}